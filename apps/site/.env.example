# Canonical public origin used by metadata, robots.txt, sitemap.xml, and OG images.
# Supply an absolute HTTP(S) URL. Paths, queries, fragments, and trailing slashes
# are normalized away.
NEXT_PUBLIC_SITE_URL=https://www.haoln7f8.com
