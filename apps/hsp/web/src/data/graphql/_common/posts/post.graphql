query CategoriesBySlugUrl($categorySlug: String) {
  categories(
    filters: { slugUrl: { eq: $categorySlug } }
    publicationState: LIVE
    pagination: { limit: 1 }
  ) {
    data {
      id
      attributes {
        title
        slugUrl
      }
    }
  }
}
