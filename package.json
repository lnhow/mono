{
  "name": "newts",
  "version": "1.0.0",
  "main": "index.js",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "start": "turbo run start",
    "turbo": "turbo run lint"
  },
  "license": "MIT",
  "devDependencies": {
    "prettier": "^2.8.8",
    "turbo": "^1.10.3"
  }
}
