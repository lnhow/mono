{
  "name": "newts",
  "version": "1.0.0",
  "main": "index.js",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint"
  },
  "license": "MIT",
  "devDependencies": {
    "eslint": "^8.42.0",
    "prettier": "^2.8.8",
    "turbo": "^1.10.3"
  }
}
